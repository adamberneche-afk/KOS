{
  "_comment": "Template only — clasp will not read a file with this name. After running 'clasp login' and 'clasp create --type standalone --title \"CAS-CCPS Studio Steps\"' for the real studio-steps Apps Script project (this is a NEW standalone project — it isn't bound to any existing spreadsheet, unlike this repo's other cas-ccps projects), copy this file to .clasp.json (dropping the .template suffix and this comment) with the real scriptId filled in. tools/clasp-sync/sync.js reads THIS file (not a real .clasp.json) as its source when generating the push folder — fill in the real scriptId here, not in a hand-created .clasp.json, so the sync stays reproducible from tracked files.",
  "scriptId": "REPLACE_WITH_REAL_SCRIPT_ID",
  "rootDir": "."
}
