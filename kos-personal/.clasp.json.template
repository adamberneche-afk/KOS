{
  "_comment": "Template only — clasp will not read a file with this name. After running 'clasp login' and either 'clasp clone <scriptId>' (pulls the existing live project — do this first, before ever pushing, so local starts as a byte-for-byte match) or 'clasp create' (only if no live project exists yet) for the real kos-personal Apps Script project, copy this file to .clasp.json (dropping the .template suffix and this comment) with the real scriptId filled in. .clasp.json is intentionally NOT committed as a real file — see meta/CLASP_AND_APPS_SCRIPT.md for the full workflow.",
  "scriptId": "REPLACE_WITH_REAL_SCRIPT_ID",
  "rootDir": "."
}
